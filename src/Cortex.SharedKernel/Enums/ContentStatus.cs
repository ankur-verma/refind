namespace Cortex.SharedKernel.Enums;

/// <summary>
/// Processing status of a content item through the AI pipeline.
/// </summary>
public enum ContentStatus
{
    Processing,
    Ready,
    Failed
}
